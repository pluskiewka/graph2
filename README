Graph2

Przy użyciu Java RMI (do rozpraszania obliczeń) oraz Swing'a do wykonania GUI klienta.

Katalogi:
* bin			- zawiera klasy w Java byte-code'ie, kompilowane przy pomocy cmd/compile.sh
* cfg			- zawiera konfigurację do uruchomienia aplikacji
* doc		- JavaDoc wygenerowany na podstawie komentarzy w kodzie
* lib			- biblioteki towarzyszące, tutaj: do logowania i testowania
* src			- źródła
* jar			- gotowe aplikacje Java'owe w słoikach.

Kompilacja:
Przy pomocy cmd/compile.sh.

Programy, w katalogu jar, znajdują się pliki *.jar, które przyjmują jako pierwszy argument adres IP. W przypadku
server.jar, jest to adres IP interfejsu, na którym nasłuchuje serwer. We wszystkich innych, to adres IP serwera.
